# F-003 implementation result

## Finding

- ID: `F-003`
- Title: lazy sections lack local failure containment
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `a6f5a804ea60a9bd22f2ffec1825ec4a98b1d424`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `3c302ef606ab1ff6133e6386b2756bf609ea14fc`
- Status: fixed and implemented
- Recorded at: `2026-07-17T23:05:00Z`

## Approved plan and deviation

The implementation followed `audit/plans/F-003.md` version 1 after finalized
F-009, F-010, F-015, and the later canonical business/public-metadata gates.
The obsolete aggregate command in the plan was mechanically rebased to
`pnpm run validate`.

One minor test-harness deviation was approved and persisted at
`audit/deviations/F-003-minor-webkit-recovery-harness.json`. Playwright WebKit
retained the synthetically rejected dynamic-import entry across native reload
and issued no second request. Isolation and Axe therefore remain required on
all three projects; reload/second-request recovery remains required on Desktop
Chromium and Mobile Chrome and is explicitly skipped only on Mobile Safari.
Production behavior, scope, dependency direction, rollback, and the native
reload recovery action are unchanged.

## Actual work

- Added feature-owned `DeferredSectionBoundary`, combining a class error
  boundary with the existing loading `Suspense` contract.
- Added concise Polish section-local error copy, an accessible native
  `Odśwież stronę` button, and user-triggered `window.location.reload()` with
  no automatic retry, analytics, or technical error disclosure.
- Replaced the local loading-only wrapper with five independent boundaries for
  effects, gallery, opinions, contact, and map.
- Preserved `efekty`, `galeria`, `opinie`, and `kontakt` anchors plus existing
  section backgrounds. The map fallback receives no new ID.
- Added boundary unit contracts for successful/loading/error paths, sibling
  isolation, anchors/backgrounds, reload control, and hidden error details.
- Added production-build chunk fault injection that proves one-shot targeting,
  local isolation, surviving eager/sibling content, Axe behavior, and bounded
  recovery.

The base-to-head diff changes exactly the five approved files. The lazy Footer,
route error UI, analytics, module loader, deferred schedule, dependencies,
business/public metadata, and all other sections remain unchanged.

## Commits

1. `2cb23ab0dc4566f03b858b4bd26b5c51baf5886d` —
   `test(home): characterize deferred chunk failure escalation (F-003)`
2. `3c302ef606ab1ff6133e6386b2756bf609ea14fc` —
   `fix(home): contain deferred section load failures (F-003)`

The second commit was recreated during in-scope E2E stabilization. Its prior
SHAs were `330af01`, `b653794`, and `7ff25f7`. Changes only reordered recovery
readiness checks, tested a connection-reset abort while investigating WebKit,
and split isolation from recovery for the approved Mobile Safari harness
limitation. No pre-F-003 commit was amended, and final history remains exactly
the two authorized commits.

## Test-first evidence

- Before production changes, a retry-free Desktop Chromium production-build
  test aborted exactly one `EffectsGallerySection-*.js` request and passed
  while observing that route-level handling removed the eager hero and later
  deferred anchors.
- Boundary tests then failed at import resolution because the component did not
  exist, establishing the expected red contract.
- After implementation, boundary plus HomePage tests passed 9/9 and the final
  retry-free Desktop Chromium isolation/recovery test passed.
- No existing test was deleted or weakened to recover green.

## Final validation

- Boundary/HomePage unit tests — 9/9 passed.
- Adjacent Desktop Chromium E2E — 24 passed, 3 expected skips.
- All-project fault spec — 5 passed, 1 approved Mobile Safari recovery skip,
  zero retries.
- Repeated all-project fault spec — 10 passed, 2 approved Mobile Safari
  recovery skips, zero retries.
- `pnpm check` — passed, including the F-008 public-metadata gate, with three
  pre-existing informational Biome notices.
- `pnpm test` — 54 files and 324 tests passed.
- `pnpm build` — typecheck, public-metadata gate, client, SSR, and Nitro passed.
- `pnpm run validate` run 1 — 324 coverage tests, dependency audit with no known
  vulnerabilities, 100 E2Es passed, 17 expected skips, zero retries.
- `pnpm run validate` run 2 — the same counts passed with zero retries.
- `git diff --check` passed.

## Before and after evidence

Before, one named optional chunk failure escalated to route-level handling and
removed eager commercial content plus unrelated deferred anchors.

After, the same injected failure leaves hero and services visible, retains the
effects anchor with a local alert, and allows gallery, opinions, contact, and
map to attach. Desktop Chromium and Mobile Chrome observe a native reload,
second matching request, restored effects heading, and removed alert. Mobile
Safari independently proves isolation and no serious Axe violations.

Normal idle mounting, direct `#kontakt` behavior, SSR/hydration, navigation,
effects, gallery, opinions, and contact flows remain green.

## Risks and uncertainty

- Error boundaries also contain render failures, intentionally making those
  defects local; production diagnostics remain outside F-003 and belong to
  F-012.
- Reload cannot repair a persistently missing asset or incoherent deployment;
  asset retention/cache recovery remains outside F-003 and belongs to F-011.
- The Playwright WebKit harness cannot prove recovery from its synthetic
  rejected-module cache. Actual transient Safari recovery remains unverified;
  the product still exposes the same user-controlled native reload.
- Finite retry-free/repeated runs cannot prove absence of future browser timing
  failures.

## Rollback

Revert `3c302ef` to restore the characterized route-level behavior. Optionally
revert `2cb23ab` to remove the characterization test. There is no dependency,
data, persistence, environment, cache schema, or irreversible migration
rollback.

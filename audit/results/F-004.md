# F-004 implementation result

## Finding

- ID: `F-004`
- Title: rendering mutates shared price history
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `1477b2341dedea4fecf4d033fdc270d9a0f74e35`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `1ca4c3f072a47159c4d1ad0e72fd11eb3721cbc9`
- Status: fixed and implemented
- Recorded at: `2026-07-17T15:58:26Z`

## Approved plan and reconciliation

The implementation followed `audit/plans/F-004.md`, merged with F-002 under
the approved cross-plan ruling. F-001's final `ServiceId` contract was consumed
directly. F-002 supplied the explicit immutable transition ledger, allowing
F-004 to remove the obsolete mutable lifecycle without creating a parallel
history owner.

## Actual work

- Removed the exported mutable history array and the record, synchronize,
  reset, and restore APIs.
- Kept the transition ledger private and exposed one defensive per-service
  accessor that clones both the point array and point objects.
- Changed `getLowestPriceInLastDays` into a pure calculation over caller-
  supplied readonly points.
- Preserved the inclusive rolling-window boundary, the price active at the
  window start, future-point exclusion, and the absent-history result.
- Removed render-time synchronization from `ExpandableServiceCard`; rendering
  now composes a defensive history read with a pure calculation.
- Replaced global-reset test fixtures and mutation assertions with immutable
  fixtures, defensive-copy tests, and customer-visible pricing assertions.

No F-005 promotion precedence, catalog data, campaign data, copy, analytics,
route, dependency, persistence, or deployment behavior changed.

## Commits

1. `ae0c12712a4e34ced652d880911659b657a62edb` —
   `test(price-history): pin disclosure behavior (F-002 F-004)`
2. `81f38c87f83bfea1d3835b7c051d722e55ea6e12` —
   `refactor(price-history): freeze generated ledger (F-002)`
3. `1ca4c3f072a47159c4d1ad0e72fd11eb3721cbc9` —
   `refactor(price-history): use immutable transition ledger (F-002 F-004)`

## Test-first evidence

Behavior-pinning tests established the disclosure window before implementation.
The final immutable-boundary contract produced 18 expected failures against
the intermediate frozen design, covering the old mutation exports, shared
object exposure, pure calculation signature, and card collaboration. The same
contracts passed after implementation without removing existing coverage.

## Final validation

- Focused price-history and card tests — 20/20 passed.
- Adjacent service, promotion, history, and type contracts — 49/49 passed.
- `pnpm check` — passed with only three pre-existing informational notices in
  `AboutProcessStep.tsx` and `useCountUp.ts`.
- `pnpm test` — 50 files and 270 tests passed.
- `pnpm build` — typecheck plus client, SSR, and Nitro builds passed.
- Relevant promotion/service E2E — 12/12 passed.
- `pnpm test:e2e` — 91 passed and 14 expected skips.
- `pnpm run validate` — exited 0 through check, build, coverage-enabled unit
  tests, dependency audit, and the full E2E suite.
- Repository search found no remaining mutation helpers, direct exported
  history import, or render-time history write.
- `git diff --check` passed for the implementation range.

## Before and after evidence

Before, a read-looking `useMemo` mutated a module-level singleton during card
render. React retries, SSR process lifetime, and render order could therefore
affect later reads, while tests required global reset coordination.

After, the data module owns an immutable private ledger, callers receive
defensive copies, the library calculates solely from explicit input, and card
rendering performs no history write. The existing October 2025 hydrogen-
cleaning disclosure remains `Najniższa cena (30 dni): 200 zł`.

## Deviations, risks, and uncertainty

- The approved F-002 and F-004 plans were executed as one serial unit because
  their final module boundary is inseparable; this was the approved cross-plan
  resolution, not a scope expansion.
- The implementation agent reached its account usage limit after completing
  the code and focused validation. The primary orchestrator performed the
  prescribed full validation and commit from the same reviewed working tree.
  This is a process-continuity note, not a code or behavior deviation.
- The immutable ledger is repository-owned rather than externally persisted;
  H02 explicitly accepted that provenance for this workflow.
- Promotion overlap and precedence remain owned by F-005.
- No new finding or unresolved implementation defect was observed.

## Rollback

Revert all three merged-unit commits in reverse order. F-002 and F-004 share
the final data boundary and should be rolled back together. No persisted data,
database, cache, dependency, deployment, or migration rollback is required.


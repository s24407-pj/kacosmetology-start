# F-008 implementation result

## Finding

- ID: `F-008`
- Title: salon/location facts have multiple owners
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `c4e3a969679ca1f44f84681d7cdd99e9cea60c60`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `0f3367486de2ccefdd7eb19c138a0a24289d73e2`
- Approved plan: `audit/plans/F-008.md` version 4
- Status: fixed and implemented
- Recorded at: `2026-07-17T21:45:00Z`

## Approved plan and deviations

The implementation began under approved plan version 1 and completed under
human-approved version 3. Three gated deviations were persisted and approved:

1. `audit/deviations/F-008-v2-generator-loader-approval.json` replaced the
   invalid native-Node TypeScript import assumption with a minimal no-listen
   loader using the already declared Vite API.
2. `audit/deviations/F-008-scope-opening-hours-test-approval.json` added one
   import/reference-only migration in `src/libs/openingHours.test.ts` so the
   legacy `contact.ts` owner could be deleted coherently.
3. `audit/deviations/F-008-v3-practitioner-genitive-approval.json` added one
   explicit canonical genitive name form to preserve `llms.txt` bytes and
   Polish grammar without duplicating identity or adding an inflection system.
4. `audit/deviations/F-008-v4-locality-and-read-errors-approval.json` added one
   canonical locality locative form after fresh review and tightened metadata
   reads so only `ENOENT` is treated as a missing artifact.

No new dependency, route, location, provider, persistence, network lookup,
public business fact, JSON-LD identity, or runtime metadata generation was
introduced.

## Actual work

- Added one typed `BusinessProfile` with canonical brand and primary salon
  location identity, phone, postal address, booking target, schedule,
  coordinates, map URL, service area, email, social profiles, logo, and site
  origin.
- Added checked location accessors plus narrow derived `brand` and
  `primarySalonLocation` exports.
- Deleted `src/data/contact.ts`, its flattened aggregate types, and every
  `@data/contact` consumer; no compatibility singleton remains.
- Added pure contact-link and `BeautySalon` JSON-LD adapters. The latter reuses
  F-014's opening-hours projection and preserves the existing public `@id`.
- Migrated route metadata, map, hero, contact, footer, phone, booking,
  promotion, service, social rail, and Plausible domain sourcing to canonical
  data while preserving route-owned prose and price range.
- Added deterministic renderers and synchronization logic for `llms.txt`,
  robots, sitemap, and web manifest.
- Added a thin generator/check wrapper that loads the real TypeScript graph
  through a minimal Vite middleware server with `configFile: false`, opens no
  listener, and always closes in `finally`.
- Added `generate-public-metadata` and read-only
  `check:public-metadata`; both `check` and `build` reject stale committed
  metadata. README and AGENTS document the ownership convention.
- Decoupled E2E page-object identity from mutable business values while keeping
  one intentional cross-surface exact parity contract.
- After fresh review, restored exact pre-F-008 map/Footer grammar and removed
  copied locative locality facts from route/public prose through
  `SalonLocation.localityLocative`.
- Changed metadata synchronization to propagate directory, permission, I/O,
  and all other non-`ENOENT` read failures unchanged.

The base-to-head diff changes only the plan's expected files, approved
mechanical E2E/CTA files, and the separately approved F-014 test import. Of the
four committed public files, only `site.webmanifest` changed, and only by JSON
key ordering; its values and semantics are unchanged.

## Commits

1. `3db867ef72b0c58f71db15e4706147f25bcef409` —
   `test(business): pin current location output across UI and SEO (F-008)`
2. `8719ff8e9462c6732388c47fdc8359a05ca56673` —
   `feat(business): add canonical typed brand and salon profile (F-008)`
3. `44217ca2a7f7f80ce6f7ae915ad150c9718695a4` —
   `feat(metadata): add business projection adapters (F-008)`
4. `20c8a64f3b602b9074c6b75b1a7ee3a1f9eeb8d6` —
   `refactor(seo): derive structured data and map from salon profile (F-008)`
5. `a74a8f4ba2f8ceb54ae2cbd85a1b8bdd5c67751e` —
   `refactor(contact): migrate consumers to canonical business data (F-008)`
6. `cdfeb37799dc4886f0a2364d0677548f2ad2169d` —
   `fix(metadata): model practitioner genitive form (F-008)`
7. `6385559ff84f3a5a40398423dbbf992d9c08231c` —
   `build(metadata): generate and verify public business metadata (F-008)`
8. `44d325f975d749a27f3c52a5797d07829260a8fd` —
   `test(e2e): centralize business-data parity assertions (F-008)`
9. `5d5c9151fa117fa9883ede7b1540173859d07040` —
   `fix(business): model locality locative form (F-008)`
10. `0f3367486de2ccefdd7eb19c138a0a24289d73e2` —
    `fix(metadata): propagate non-ENOENT read failures (F-008)`

## Test-first and checkpoint evidence

- The production-free characterization checkpoint passed 33 focused tests and
  four retry-free Desktop Chromium SSR tests before ownership changed.
- The additive domain checkpoint pinned exact current facts, unique/stable
  location IDs, primary resolution, URLs, coordinates, country, and explicit
  service area.
- Adapter tests first specified contact composition, complete JSON-LD,
  optional socials, changed-fixture propagation, deterministic escaping and
  newline behavior, and stale/write decisions.
- The metadata loader's first real `--check` intentionally exited 1 and listed
  stale paths without modifying them; later clean checks passed repeatedly.
- Consumer migration passed 85 focused tests and typecheck before deleting the
  old contact owner.
- The grammatical correction pinned nominative and genitive values, modified-
  fixture projection/escaping, and byte-identical committed llms output.
- Page objects and E2E parity were migrated only after production ownership and
  generated artifacts were green. No existing test was deleted or weakened.
- The first fresh review then demonstrated two failures: exact map/Footer
  grammar had regressed because nominative locality was used after `w`, and an
  `EISDIR` metadata read resolved as ordinary staleness. Version 4 tests first
  failed on both behaviors, then passed after the two focused corrections.

## Final validation

- Focused post-review F-008 unit/component group — 13 files and 74 tests
  passed.
- `pnpm check:public-metadata` — passed repeatedly after generation.
- `pnpm typecheck` — passed.
- `pnpm check` — passed with three pre-existing informational Biome notices.
- `pnpm test` — 53 files and 319 tests passed.
- `pnpm build` — typecheck, metadata drift gate, client, SSR, and Nitro passed.
- Contact/SSR Desktop Chromium E2E — 12/12 passed.
- Five planned E2E files — 57 passed with 15 intentional project skips.
- `pnpm run validate` — passed through check, build, 319 coverage tests,
  dependency audit at the configured high-severity threshold, and the complete
  111-test E2E matrix under configured pass/skip expectations.
- Coverage was 88.2% statements, 83.6% branches, 84.01% functions, and 88.88%
  lines.
- `git diff --check` passed.

## Before and after evidence

Before, contact and location facts were copied across the flattened contact
aggregate, root JSON-LD, map, UI surfaces, analytics configuration, E2E
selectors, and four manually maintained public files.

After, mutable brand/location facts have one typed edit home. UI and analytics
consume narrow canonical exports; JSON-LD and public files are deterministic
projections; build/check prevent generated drift; and exact cross-surface E2E
parity is intentional rather than embedded in unrelated locators. Searches
found no legacy contact aggregate/import and raw operational values remain only
in canonical data plus deliberate characterization/domain tests.

The nominative locality remains the postal, Hero, document-title, JSON-LD, and
service-area identity. The explicit locative is used only by existing Polish
map, Footer, llms, and Open Graph prose. Metadata synchronization treats a
missing file as stale but exposes every other filesystem failure.

## Risks and uncertainty

- Canonical ownership increases the importance of reviewing edits to the one
  profile; domain and cross-surface parity tests pin every current fact.
- `practitionerName` is nominative and `practitionerNameGenitive` is the one
  separately evidenced form for the existing llms sentence. Both must be
  updated together after a name change; no other grammatical cases are
  inferred.
- `address.locality` is nominative and `localityLocative` is the one separately
  evidenced location form. No other cases or inflection rules are inferred.
- The generator depends on the declared Vite programmatic SSR API and must be
  rechecked during Vite upgrades. It deliberately does not load the full
  TanStack/Nitro configuration.
- Human decision H-05 states that no out-of-repository consumers are known.
  Unknown private-path consumers and external crawler caches cannot be
  enumerated from this checkout.
- External business-profile freshness was not verified against Booksy, Google
  Maps, social profiles, the live site, or Schema.org validators; repository
  output was preserved.
- The first Playwright attempt could not bind a local listener in the sandbox;
  the identical command passed when executed with the required environment
  permission. No code or scope changed.
- The first fresh review failed F-008 on the map/Footer locality regression and
  masked non-`ENOENT` metadata reads. Both are corrected in `5d5c915` and
  `0f33674`; final acceptance still requires a second fresh review.

## Rollback

Revert the ten F-008 commits in reverse order. First revert the metadata error
policy and locality correction, then the E2E parity commit, generated
files/package/docs together, practitioner-name grammatical correction,
consumer migration, SEO/map migration, adapters/generator, canonical profile,
and characterization. Deploy public assets, SSR, and client chunks from one
coherent revision. There is no database, cache schema, persistent client state,
environment, or irreversible migration rollback.

# F-002 implementation result

## Finding

- ID: `F-002`
- Title: default price history is regenerated from current configuration
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `1477b2341dedea4fecf4d033fdc270d9a0f74e35`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `1ca4c3f072a47159c4d1ad0e72fd11eb3721cbc9`
- Status: fixed and implemented
- Recorded at: `2026-07-17T15:58:26Z`

## Approved plan and reconciliation

The implementation followed `audit/plans/F-002.md`, merged with the approved
F-004 plan under the recorded cross-plan ruling. F-001 had already established
stable `ServiceId` identity and F-007 had already validated the production
campaign set, so the ledger uses the final identity contract and was
materialized from a validated repository configuration.

Human decision H02 accepted the repository-derived materialized ledger as the
available historical source. No external business-owned ledger exists in the
repository, so the result is a stable record of the pinned configuration, not
a claim of independently observed historical provenance.

## Actual work

- Added behavior-pinning tests for rolling-window disclosure boundaries.
- Mechanically froze the exact generated 416-point history as literals in a
  dedicated checkpoint and verified byte-equivalent output.
- Normalized that snapshot to 82 effective-price transitions, removing
  unchanged checkpoints and adding explicit post-campaign restorations,
  including 2027 restorations where the price changes.
- Removed catalog and promotion imports from the historical ledger so future
  edits cannot rewrite past points at module load.
- Kept the committed ledger private and keyed by readonly `ServiceId` values.
- Made `getServicePriceHistory(serviceId)` return defensive copies.
- Made the rolling-window calculation consume explicit readonly history,
  exclude future points, include the price active at the window start, and
  avoid mutating its input.

The merged F-004 implementation also removed the superseded synchronization
and mutable singleton lifecycle. The six changed production/test files are the
exact union approved by the cross-plan ruling.

## Commits

1. `ae0c12712a4e34ced652d880911659b657a62edb` —
   `test(price-history): pin disclosure behavior (F-002 F-004)`
2. `81f38c87f83bfea1d3835b7c051d722e55ea6e12` —
   `refactor(price-history): freeze generated ledger (F-002)`
3. `1ca4c3f072a47159c4d1ad0e72fd11eb3721cbc9` —
   `refactor(price-history): use immutable transition ledger (F-002 F-004)`

## Test-first evidence

The behavior-pinning checkpoint passed against the old implementation and
fixed the intended disclosure rules before the data migration. After the
mechanical freeze, the exact 416-point output was compared with the generated
baseline. The final-contract tests then produced 18 expected failures against
the frozen mutable design before passing with the normalized immutable ledger.

## Final validation

- Focused price-history and card tests — 20/20 passed.
- Adjacent service, promotion, history, and type contracts — 49/49 passed.
- `pnpm check` — passed with only three pre-existing informational notices in
  `AboutProcessStep.tsx` and `useCountUp.ts`.
- `pnpm test` — 50 files and 270 tests passed.
- `pnpm build` — typecheck plus client, SSR, and Nitro builds passed.
- Relevant promotion/service E2E — 12/12 passed.
- `pnpm test:e2e` — 91 passed and 14 expected skips.
- `pnpm run validate` — exited 0 through check, build, coverage-enabled unit
  tests, dependency audit, and the full E2E suite.
- Searches found no catalog/promotion dependency in the ledger and no
  remaining price-history mutation API.
- `git diff --check` passed for the implementation range.

## Before and after evidence

Before, 416 generated snapshots were reconstructed from today's catalog and
campaign configuration. Campaign ends were implicit, many points did not
represent changes, and later edits could rewrite the apparent past.

After, 82 explicit effective-price transitions are committed as immutable
facts. Restorations are visible, future points remain scheduled rather than
current, and catalog or promotion edits have no import edge that can
recalculate the ledger.

## Deviations, risks, and uncertainty

- The approved F-002 and F-004 plans were executed as one serial unit because
  their final module boundary is inseparable; this was the approved cross-plan
  resolution, not a scope expansion.
- The implementation agent reached its account usage limit after completing
  the code and focused validation. The primary orchestrator performed the
  prescribed full validation and commit from the same reviewed working tree.
  This is a process-continuity note, not a code or behavior deviation.
- The ledger is repository-derived per H02. It cannot prove facts outside the
  repository and must be updated deliberately when real effective prices
  change.
- No F-005 overlap, stacking, or precedence behavior was introduced.
- No database, browser storage, deployment state, migration, or cache is
  involved.

## Rollback

Revert all three merged-unit commits in reverse order. The mechanical freeze
is not intended as a standalone final architecture, so F-002 and F-004 should
be rolled back together. No persisted data, database, cache, dependency,
deployment, or migration rollback is required.


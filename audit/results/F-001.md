# F-001 implementation result

## Finding

- ID: `F-001`
- Title: display names are service identifiers
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `1db4cfbd1930e748d630860d1c21cd35ffb558b2`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `1b505b276dd2c5d3c8bb0e65d0343fed4a9b7ccf`
- Status: fixed and implemented
- Recorded at: `2026-07-16T22:38:27Z`

## Approved plan and reconciliation

The implementation followed `audit/plans/F-001.md` on the approved fix branch.
The execution base was intentionally ahead of the pinned SHA because F-015,
F-010, and F-009 had already landed in the required roadmap order. Their retry
evidence, shared render-time/readiness behavior, and canonical
`pnpm run validate` command were preserved.

The Stage 2.3 cross-plan ruling superseded one temporary test detail from the
individual plan: F-001 does not leave a whole-production promotion-reference
validator in `promotion.test.ts` or `services.test.ts`. F-001 retains catalog
ID format/uniqueness and ID-targeting behavior; F-007 remains the future
canonical whole-configuration validator. No F-013 taxonomy/view-mode rename,
F-002/F-004 history redesign, F-005 precedence work, or F-007 validator was
implemented.

## Actual work

- Added the canonical `ServiceId` domain type and required `Service.id` field.
- Assigned the exact approved stable ID to all 26 catalog records and added
  `getServiceById` beside the catalog as the single ID-to-presentation lookup.
- Migrated all 14 service-scoped production promotion records from copied names
  to `serviceIds`, while retaining catalog-name fallback copy and the existing
  generic `wybrane zabiegi` response for an unresolved runtime ID.
- Migrated generated price-history entries, clone/reset logic, helper APIs, and
  card calls from display names to `serviceId` without changing point values,
  ordering, window rules, or render-time synchronization behavior.
- Migrated expanded-card state, toggle inputs, React keys, DOM IDs, and
  service-card analytics to stable IDs. `Service.name` remains presentation
  copy for headings and accessible labels.
- Added behavior-first coverage for current Polish promotion copy, card
  accessibility association, and one-card expansion before production edits.
- Added catalog identity, rename-resilient campaign targeting, unknown-ID
  fallback, generated-history identity, ID-keyed expansion, DOM, history-call,
  and analytics contracts.

The final implementation changes exactly the 14 approved expected files: 291
insertions and 131 deletions. No possible-mechanical E2E/page-object file was
needed. No lockfile, dependency, generated source, binary, snapshot, migration,
route, price, service name, promotion result, or visible clinical copy changed.

## Commits

1. `0448fb73f96579bef1bd99eb27ecfea27cd8c75a` —
   `test(services): pin service presentation before identity migration (F-001)`
2. `af5474a725df1fdb1fcd0d7b50b6ac6e2cc09688` —
   `refactor(services): add stable catalog service ids (F-001)`
3. `88d9461395e2ff08f0c7f090408a7b78f8115a8e` —
   `refactor(promotions): reference services by stable id (F-001)`
4. `0f91b3e6bcc92dfac1e470f1e70d8b617cb88b40` —
   `refactor(price-history): key service history by stable id (F-001)`
5. `42a27ae14a0c26cf34207751a836578cca0c1a98` —
   `refactor(services-ui): use stable ids for state dom and analytics (F-001)`
6. `1b505b276dd2c5d3c8bb0e65d0343fed4a9b7ccf` —
   `style(services): format stable identity migration (F-001)`

The sixth commit contains only Biome's mechanical formatting/import ordering
after the first repository-wide check. It stays within already approved F-001
paths and changes no behavior.

## Test-first checkpoints

- Behavior pin: 3 files and 28 tests passed before production changes.
- Catalog schema/fixtures: 4 files and 30 tests passed; typecheck passed.
- Promotion references: 4 files and 30 tests passed; typecheck passed.
- Price-history identity: 3 files and 15 tests passed; typecheck passed.
- UI/DOM/analytics identity: 2 files and 9 tests passed; typecheck passed.

No existing test was deleted to recover green. The audit's original focused
four-file service-domain sample increased from 37 to 40 passing tests.

## Final validation

- `pnpm check` — passed. It reports only three pre-existing informational
  notices in `AboutProcessStep.tsx` and `useCountUp.ts`; neither file changed.
- `pnpm test` — 48 files and 249 tests passed.
- `pnpm build` — typecheck and the client, SSR, and Nitro builds passed.
- Relevant promotion/services E2E with `--retries=0` — 15/15 passed.
- Full E2E with `--retries=0` — 91 passed and 14 expected skips.
- `pnpm run validate` — passed twice through check, typecheck/build,
  coverage-enabled unit tests (48 files, 249 tests), high-severity dependency
  audit, and the full E2E suite (91 passed, 14 expected skips). No recovered
  retry or flaky classification appeared.
- `pnpm audit --audit-level high` — no known vulnerabilities found.
- Final legacy-identity search found no `serviceName`, `serviceNames`,
  name-derived detail ID, name-keyed expansion/key, or legacy service-card
  analytics property under tracked source/tests. Remaining `service.name`
  uses in the affected domain are presentation assertions/rendering only.
- Final source diff from the execution base is exactly the 14 approved files.

## Before and after evidence

Before:

- `Service.name` was the only catalog identity.
- Promotion targets copied Polish names and matched them exactly.
- Price history, expanded state, React keys, DOM IDs, and analytics used names.

After:

- The catalog is the canonical home of 26 stable ID-to-name mappings.
- Promotion and history references use `ServiceId` and resolve presentation
  through the catalog only where visible copy is required.
- UI state, reconciliation, DOM association, history calls, and analytics use
  the same stable ID, while visible and accessible customer copy remains the
  unchanged Polish service name.
- A copied service with the same ID and a changed display name remains eligible
  for its campaign; two same-name fixtures with distinct IDs expand separately.

## Operability, risks, and uncertainty

- The service-card analytics property intentionally changes from `service` to
  `serviceId`. Human approval recorded no known external consumer. Historic
  events retain the old property, and an unknown saved Plausible report may
  require manual adjustment after deployment.
- External DOM automation that depended on the old lossy name-derived detail
  ID may require adjustment; no repository consumer was found.
- There is no persisted price-history store at this revision, so no data
  migration is required. F-001 intentionally preserves the mutable generated
  history lifecycle pending the approved F-002/F-004 unit.
- Catalog format/uniqueness and rename-resilient targeting are covered now.
  Complete production configuration integrity remains intentionally deferred
  to F-007's one canonical validator.
- Finite local browser validation does not prove unknown external analytics or
  DOM consumers are absent. It does establish unchanged in-repository visible
  prices, promotions, names, accessibility, and browser flows.

## Rollback

Revert the six implementation commits in reverse order. Promotion and history
commits must be reverted before the catalog/schema commit. No database, cache,
localStorage schema, dependency, route, or deployment migration must be
reversed. Analytics events already emitted with `serviceId` cannot be removed;
a deployed rollback creates a mixed telemetry interval that must be considered
when interpreting reports.

# F-005 implementation result

## Finding

- ID: `F-005`
- Title: overlapping-promotion precedence is undefined
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `54a783c3ce358d5a37acc5d80a74b9e2e5b78e22`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `c278186e667c9dbd5280a90ee7d7e9b69acf71be`
- Status: fixed and implemented
- Recorded at: `2026-07-17T17:12:57Z`

## Approved plan and policy

The implementation followed `audit/plans/F-005.md` version 2 after the final
F-001, F-002/F-004, F-007, F-010, and F-013 contracts. Human decision H-01
approved simultaneous campaigns, plural presentation, non-stacking discounts,
highest-percentage precedence, and deterministic ties by earlier start then
lexicographically smaller promotion ID.

F-002/F-004 remains authoritative for historical facts. F-005 owns only
present-time active selection, service resolution, rounded current price, and
plural presentation.

## Actual work

- Added green characterization for the existing September/October single-
  campaign messages, pricing, CTA, dismissal, analytics, and immutable history
  collaboration.
- Added `resolveServicePromotion(service, activePromotions)` as the canonical
  pure resolver for applicability, non-stacking precedence, tie-breaking, and
  rounded integer current price.
- Kept `getAllActivePromotions` as the plural date/cardinality query and removed
  the singular `getActivePromotion` export after all consumers migrated.
- Updated `ServicesSection` to render every campaign summary and a de-duplicated
  union of services with a non-null resolution.
- Updated `ExpandableServiceCard` to consume the canonical winning promotion
  and price while preserving the defensive historical read and pure rolling
  query.
- Updated `PromotionBanner` to present every active campaign in one dismissible
  status region, retain campaign-specific CTA analytics, and emit dismissal
  once for each active promotion.

The base-to-head diff changes exactly the eight files approved by the plan.
Production campaign records, history modules, dependencies, routes, E2E
fixtures, analytics schema, and persisted state are unchanged.

## Commits

1. `a690d79287b5e25f203de33a7a51a6384778bf0e` —
   `test(promotions): pin single-campaign behavior before resolution change (F-005)`
2. `023533c842911adaf95eec9c189aee27ec71f4d5` —
   `feat(promotions): add deterministic current service promotion resolver (F-005)`
3. `744752e3480102b00d333574023b9aa9b44bda55` —
   `refactor(services): resolve and present simultaneous promotions (F-005)`
4. `c278186e667c9dbd5280a90ee7d7e9b69acf71be` —
   `refactor(promotion-banner): present all active campaigns (F-005)`

## Test-first evidence

- The pre-change characterization checkpoint passed 38 tests across the four
  approved suites.
- Resolver contracts first failed 6 of 31 tests because the resolver did not
  exist, then passed 31/31 after implementation.
- Services/card plural contracts first failed seven tests against legacy
  singular selection, then passed 43 focused tests after migration.
- Banner plural contracts first failed six tests against the legacy singular
  selector, then the complete focused F-005 set passed 50 tests.
- No existing test was removed to recover green, and no live overlapping
  campaign or test-only production seam was introduced.

## Final validation

- Focused F-005 suites — 4 files and 50 tests passed.
- F-002/F-004 history regression — 2 files and 16 tests passed.
- `pnpm check` — passed with the same three pre-existing informational notices
  outside scope.
- `pnpm test` — 50 files and 283 tests passed.
- `pnpm build` — typecheck plus client, SSR, and Nitro builds passed.
- Relevant promotion E2E — 12/12 passed.
- Exact F-010 SSR/hydration rerun with `--retries=0` — 3/3 passed.
- `pnpm run validate` — exited 0 through check, build, 283 coverage tests,
  dependency audit, and the full E2E suite (91 passed, 14 expected skips).
- The first aggregate run exposed JSX comment boundaries in the raw SSR
  promotion message. The approved banner file was corrected to preserve the
  message as one string, the final commit was amended, and the complete
  aggregate gate then passed.
- Searches found no `getActivePromotion`, history mutation/reset/sync API, or
  current promotion import in the history modules.
- The four validation-only history files are byte-identical to the execution
  base, and `git diff --check` passed.

## Before and after evidence

Before, three consumers selected the first active campaign and cards separately
implemented applicability and discount arithmetic. A future overlap would hide
later campaign messages and make configuration order an implicit pricing rule.

After, plural selection presents every active campaign, one order-independent
resolver selects the largest single applicable discount with explicit ties,
services are de-duplicated, and rounded current price has one canonical owner.
Existing non-overlap production output remains unchanged, including the
October `250 zł` to `200 zł` service price and lowest-price disclosure.

## Deviations, risks, and uncertainty

- No plan, scope, architecture, sequencing, or historical-ownership deviation
  occurred. Two transient implementation defects (a missing `%` suffix and a
  nullable narrowing) were corrected before the independently green Step 3
  commit.
- No production overlap exists, so overlap behavior is exercised with compact
  synthetic domain/component fixtures rather than invented commercial data.
- Human decision H-05 states that no out-of-repository consumers are known.
  Unknown external DOM automation and Plausible dashboards cannot be verified
  from this repository; event names and the scalar `promotionId` schema remain
  unchanged.
- If a live overlap later relies on this policy, removing the resolver without
  first removing or rescheduling the overlap could change the visible offer.
- No new finding was observed.

## Rollback

Revert the four F-005 commits in reverse order: banner/API removal, services
migration, additive resolver, then characterization. If a live overlap exists,
remove or reschedule it before rollback. The immutable historical ledger is
not rolled back, and no database, cache, persisted state, dependency, or
migration rollback is required.

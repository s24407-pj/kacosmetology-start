# F-013 implementation result

## Finding

- ID: `F-013`
- Title: catalog taxonomy is conflated with services-section views
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `115c224bc9badbf582176ac71d88a42e9bf390d2`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `334626fd15cebc2992c2b7e9ef5324d454cf9440`
- Status: fixed and implemented
- Recorded at: `2026-07-16T22:53:14Z`

## Approved plan and reconciliation

The implementation followed `audit/plans/F-013.md` on the approved fix branch
after F-001. The existing `ServiceId`, ID-keyed UI behavior, and F-010 render
time/readiness contract were preserved. No F-005 promotion-resolution policy,
F-007 production validation, or F-001 identity change was included.

The execution task explicitly required a committed pre-change behavior pin, so
the planned two implementation boundaries were preceded by a green test-only
commit. Rendered option order, filtering, expansion, `aria-pressed`, special
views, and the legacy voucher hash were characterized before production edits.
The compile-time contract was added atomically with the catalog boundary; its
failure against the old schema is statically direct because the old `Service`
had no `catalogCategory`, but no separate red typecheck transcript was retained.

## Actual work

- Added `ServiceCatalogCategory` with exactly `Kosmetologia`, `Trychologia`,
  `Oprawa oka`, and `Online` and made it the type of
  `Service.catalogCategory`.
- Mechanically migrated all 26 catalog records and every approved service
  fixture without changing service IDs, values, order, prices, duration, or
  copy.
- Typed category-scoped promotion applicability with the catalog taxonomy and
  compared it with `service.catalogCategory`; promotion selection, scope copy,
  and ID-targeted behavior remained unchanged.
- Added a local `ServiceView` for the four catalog choices plus `Vouchery` and
  `Promocje`, then applied the approved view-oriented local naming matrix.
- Moved `CategoryButton` and its tests with `git mv` to the feature-owned
  `ServiceViewButton`, removed the obsolete shared barrel export, and updated
  the one direct production consumer.
- Renamed the Playwright page-object lookup to `getViewButton` and updated all
  exact consumers and developer-facing special-view descriptions without
  changing locators or labels.
- Added a type contract proving that `Service.catalogCategory` uses the four
  literal taxonomy values and excludes `Vouchery` and `Promocje`.

The implementation changes 16 approved logical paths: 164 insertions and 95
deletions, including the two history-preserving file moves. No compatibility
alias, duplicate field, lockfile, dependency, generated source, binary,
snapshot, migration, promotion policy, validation gate, or public copy changed.

## Commits

1. `b9f8a676a10af0ef33335f27e34f5aa13c6f4e0b` —
   `test(services): pin selector behavior before taxonomy split (F-013)`
2. `679b66c565efb7b7c894e011fde3759d412011a3` —
   `refactor(catalog): separate service taxonomy from offer views (F-013)`
3. `334626fd15cebc2992c2b7e9ef5324d454cf9440` —
   `refactor(services): align selector ownership and view naming (F-013)`

## Behavior-preservation evidence

- The default view remains `Kosmetologia`; the exact six labels and their
  order remain unchanged.
- Selecting `Trychologia` flips the same pressed state, hides cosmetology
  fixtures, shows the trychology fixture, preserves consultation grouping, and
  retains ID-keyed single-card expansion.
- `Vouchery` still renders the voucher panel without service cards, and
  `Promocje` still renders the same active banner/filter or inactive empty
  state.
- `#services-vouchery` still selects vouchers, clears the hash through replace
  navigation, and scrolls to `#zabiegi`.
- The public group label `Kategorie zabiegów`, button labels, `aria-pressed`,
  button element/classes/click contract, hashes, and service-card identities
  are unchanged.
- Category-scoped promotion behavior still matches the same four catalog
  values; service-scoped campaigns remain keyed by F-001 `ServiceId`.

## Validation

- Old-symbol searches — no code match for `ServiceCategory`,
  `Service.category`, `CategoryButton`, `activeCategory`,
  `setActiveCategory`, `handleCategoryChange`, `isVoucherCategory`,
  `isPromotionCategory`, `filteredServices`, or `getCategoryButton`; all 26
  catalog records use `catalogCategory`, and `ServiceView` has one local home.
- `pnpm typecheck` — passed.
- Focused Vitest command — 5 files and 35 tests passed.
- Relevant Playwright command — 17 passed and 1 expected skip across the
  configured projects.
- `pnpm check` — passed with only the three pre-existing informational notices
  in `AboutProcessStep.tsx` and `useCountUp.ts`; neither file changed.
- `pnpm build` — typecheck plus client, SSR, and Nitro builds passed.
- `pnpm test` — 49 files and 252 tests passed.
- `pnpm test:e2e` — 91 passed and 14 expected skips; no recovered retry or
  flaky classification appeared.
- `pnpm run validate` — passed through check, typecheck/build,
  coverage-enabled unit tests, the high-severity dependency audit, and the full
  E2E suite (91 passed, 14 expected skips).

## Risks and uncertainty

- The accessible group label intentionally retains category wording because
  the approved finding preserves public Polish copy and accessibility
  contracts. Code ownership and types now distinguish the broader six-choice
  view from the four-value catalog taxonomy.
- The simple local union is intentionally not a tagged union. It has one owner
  and no compatibility alias; a future materially different view can introduce
  a discriminator when branch-specific data requires one.
- Finite local tests cannot prove unknown external DOM consumers do not exist,
  but human approval records no known external consumers and every repository
  locator continues to use the same accessible labels.

## Rollback

Revert the three F-013 commits in reverse order. Revert the view/component move
before reverting the catalog boundary. No database, storage, cache, dependency,
analytics, route, promotion-policy, or deployment migration must be reversed.

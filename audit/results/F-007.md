# F-007 implementation result

## Finding

- ID: `F-007`
- Title: production campaign configuration is not validated as a set
- Pinned repository SHA: `777412c4e8cbc06336be60b0cbbd4ce2554f059e`
- Execution base: `77e4f165a6d088e8fd0869d58c6d21f5eb0237d9`
- Implementation branch: `audit/fixes-2026-07-16`
- Final implementation HEAD: `157532c913339b41e067bf61ce4034fab10dda80`
- Status: fixed and implemented
- Recorded at: `2026-07-16T23:09:10Z`

## Approved plan and reconciliation

The implementation followed `audit/plans/F-007.md` after the approved F-001
and F-013 commits. The execution base was ahead of the pinned repository SHA
because F-015, F-010, F-009, F-001, and F-013 had already landed in the
approved order. F-001's final promotion schema uses stable `serviceIds`, and
F-013's final service schema uses `ServiceCatalogCategory` and
`catalogCategory`; the validator consumes those final contracts directly.

No whole-production promotion reference assertion existed in
`promotion.test.ts` or `services.test.ts`, so there was nothing to consolidate.
Catalog ID format/uniqueness remains owned by `services.test.ts`, runtime
selection/applicability remains owned by `promotion.test.ts`, and the new
suite is the sole complete production-set gate.

## Actual work

- Added `validatePromotionConfigs(configs, catalog)` as a pure aggregate
  validator over readonly inputs.
- Added structured, deterministic issues containing an issue code,
  configuration index, promotion ID, field, and offending catalog reference
  where applicable.
- Validated non-blank and unique promotion IDs; strict `YYYY-MM-DD` date shape
  and real UTC calendar-day round trips; non-reversed valid ranges; service IDs
  that resolve exactly once; and categories present in the actual catalog.
- Precomputed promotion ID counts, service ID counts, and catalog categories
  once per invocation. The validator does not mutate, sort, normalize, or
  filter either input.
- Added focused negative contracts for all seven issue codes, impossible dates,
  ambiguous service IDs, aggregate reporting, and stable issue order.
- Added the one production integrity assertion over the real
  `promotionConfigs` and `services` arrays.
- Added behavior characterization proving both existing selector loops skip a
  malformed caller-supplied record and continue to valid active promotions.

Runtime selectors, campaign records, catalog records, prices, dates, copy,
overlap/precedence policy, price history, CI configuration, and dependencies
were unchanged. The validator is not called during import, server startup,
rendering, or selector execution.

The implementation changes exactly the three approved expected files: 399
insertions. Neither possible mechanical file (`promotion.ts` or
`services.test.ts`) was needed.

## Commits

1. `cb1a663216a0f4f14fc6939533317e4fe1b04ad8` —
   `test(promotions): pin malformed-config fail-safe selection (F-007)`
2. `157532c913339b41e067bf61ce4034fab10dda80` —
   `test(promotions): gate production campaign configuration (F-007)`

## Test-first evidence

- The runtime fail-safe characterization passed before validator work: one
  file and 23 tests.
- The new validator contract first failed because
  `./promotionValidation` did not exist. Vitest collected no tests and reported
  only the expected unresolved import.
- After implementation, the validator suite passed 11 tests.
- The consolidated service/promotion/validator run passed 3 files and 36
  tests. No existing test was removed to recover green.

## Final validation

- Focused promotion and validator tests — 2 files and 34 tests passed.
- `pnpm check` — passed with only the three pre-existing informational notices
  in `AboutProcessStep.tsx` and `useCountUp.ts`; neither file changed.
- `pnpm typecheck` — passed.
- `pnpm build` — typecheck plus client, SSR, and Nitro builds passed.
- `pnpm test` — 50 files and 265 tests passed.
- Relevant promotion E2E — 12/12 passed across Desktop Chromium, Mobile
  Chrome, and Mobile Safari.
- `pnpm test:e2e` — 91 passed and 14 expected skips; no recovered retry or
  flaky classification appeared.
- `pnpm run validate` — exited 0 through check, typecheck/build,
  coverage-enabled unit tests (50 files, 265 tests), high-severity dependency
  audit, and the full E2E suite (91 passed, 14 expected skips). The command was
  rerun once to capture an explicit exit status after the first run's terminal
  output exceeded the display budget.
- Repository search found one production validator definition, one complete
  production-set invocation, and no runtime or second validation consumer.
- `git diff --check` passed for the implementation range.

## Before and after evidence

Before, malformed custom configuration was silently skipped by design, but the
same fail-safe behavior allowed malformed checked-in production campaigns to
pass repository acceptance without a complete integrity assertion.

After, runtime selection remains fail-safe and available, while the normal
unit/coverage gate rejects checked-in campaign sets with malformed or
impossible dates, reversed ranges, invalid IDs, or unresolved catalog targets.
Every issue is reported in stable source order so maintainers can correct
multiple mistakes in one pass.

## Deviations, risks, and uncertainty

- No plan or scope deviation occurred. Biome-required formatting was applied
  only to the two new approved files before the validator commit.
- No duplicate production-reference assertion existed after F-001, so the
  approved optional consolidation was unnecessary.
- `unknown-service` also reports an ID that resolves more than once in a
  malformed caller-supplied catalog, matching the approved requirement that a
  reference resolve exactly once. Catalog uniqueness remains independently
  enforced by F-001's catalog test.
- The gate is enforced by unit and coverage commands, not by `pnpm build`
  alone. This is the approved acceptance boundary and is exercised by both CI
  and `pnpm run validate`.
- No overlap, recurrence, discount-bound, copied-price, or history policy was
  inferred. Those remain outside F-007.

## Rollback

Revert the two implementation commits in reverse order. This removes the
test-time gate and then its runtime fail-safe characterization without changing
production data or runtime selector behavior. No database, cache, persisted
state, dependency, deployment, or migration rollback is required.
